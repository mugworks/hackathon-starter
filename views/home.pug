extends layout

block content
  img(src= "css/images/nwmmb.png")
  h1.col-sm-6 northwest Mothers Milk Bank
  hr
  div
    .row
      .col-sm-6
        form(action="/locations", method="get")
          button(class="btn btn-primary") Schedule Drop-off
  div
    .row
      .col-sm-6
        form(action="/api", method="get")
          button(class="btn btn-primary") Drop-off Check-in
